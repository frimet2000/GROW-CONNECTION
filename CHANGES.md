# מה תוקן בגרסה 2.0

הגרסה הקודמת נכתבה מול endpoint ומבנה payload ש-GROW לא מגישים, וללא אף אחת
משכבות ההגנה שתשלום דורש. להלן כל פער, מה הייתה התוצאה בפועל, והתיקון.
המקור: אינטגרציה חיה ובדוקה מול GROW (פרויקט Orbar), כולל הנחיות שהתקבלו
מתמיכת ה-API של GROW ב-08/2026 ו-09/2026.

## חורי אבטחה (קריטי)

| # | מה היה | מה זה אפשר | התיקון |
|---|---|---|---|
| 1 | ה-webhook לא אימת דבר | כל אחד ברשת יכול היה לשלוח `POST` עם `cField1` מנוחש ולסמן הזמנה כשולמה. גניבת סחורה ללא תשלום, בלי עקבות | אימות IP מול הרשימה שGROW מפרסמת + `webhookKey` בפורמטים שנושאים אותו. קריאה לא מאומתת מקבלת 200 ריק ולא חושפת אם המזהה קיים |
| 2 | הסכום נלקח מגוף הבקשה של הלקוח (`amount`) | לקוח יכול היה לשלם ₪1 על הזמנה של ₪1,400 | הסכום נקרא בצד השרת מישות ההזמנה. הלקוח מעביר `orderId` בלבד |
| 3 | ה-webhook סמך על `sum` שהגיע ב-callback | callback משוחזר או מזויף עם סכום אחר היה נסגר כתקין | השוואה מול `payment_amount_agorot` שנרשם ביצירת הקישור. אי-התאמה → סטטוס `payment_review`, לא `completed` |
| 4 | אין הגנת idempotency | GROW חוזר על callbacks; כל חזרה סגרה את ההזמנה מחדש | סגירה מתבצעת פעם אחת; callback על הזמנה סגורה מוחזר כ-`duplicate` |
| 5 | ההזמנה אותרה לפי `cField1` שהגיע מבחוץ, כמזהה ישיר | הזרקת מזהה של הזמנה אחרת | ההתאמה היא לפי מפתח שאנחנו יצרנו ושמרנו (`payment_idempotency_key`) |
| 6 | `Access-Control-Allow-Origin: *` על endpoint שמייצר חיוב | כל אתר יכול היה להפעיל אותו מהדפדפן של המשתמש | `ALLOWED_ORIGIN` |

## שגיאות פרוטוקול — הקוד לא היה עובד

| # | מה היה | התיקון |
|---|---|---|
| 7 | `https://api.meshulam.co.il/api/light/createPaymentPage` | הכתובת הזו לא מגישה את הסכימה הזו. הנכונה: `CreatePaymentLink` על `sandboxapi.grow.link` / `api.grow.link`, לפי הסביבה |
| 8 | גוף JSON | כל קריאות ה-Light API הן `multipart/form-data`. JSON לא מחזיר שגיאת content-type — השדות פשוט לא נקראים, ו-GROW עונה שגיאה מטעה |
| 9 | חסרה כותרת `x-api-key` | חובה מאז עדכון השרת של GROW (08/2026). בלעדיה הבקשה נדחית |
| 10 | payload שטוח: `sum`, `fullName`, `phone`, `cancelUrl` | תמיכת GROW פסלה את השדות האלה כ״לא רלוונטיים״ לקריאה הזו. הסכימה המתועדת: `products[data][0][price]`, `paymentLinkType`, `isActive`, `title`, `paymentTypes[0][type]`, `products[data][0][vatType]`. `cancelUrl` לא קיים בסכימה |
| 11 | `paymentNum: 1` | השדה הוא `paymentTypes[0][payments][paymentsPaymentNum]`, ו-`paymentTypes[0][type]` הוא **המחרוזת** `"payments"` — קוד מספרי נכשל ב-err.id 427 |
| 12 | לא נשלח `paymentLinkType` | חייב להיות `2`. הערך `1` שהתיעוד רומז עליו מייצר כפתור תשלום מת (תמיכת GROW, 31/08/2026) |
| 13 | `Math.round(amount)` | עיגל שקלים שלמים והשמיט אגורות — ₪149.90 נגבה כ-₪150. כל הסכומים עברו לאגורות שלמות, המרה לשקלים רק על החוט |
| 14 | לא נשלח `notifyUrl` | בלעדיו הדרך היחידה לדעת על תשלום היא ההפניה בדפדפן, שהלקוח יכול לסגור |
| 15 | `approveTransaction` לא קיים כלל | נדרש אחרי כל תשלום מוצלח. זהו הצעד שהכי הרבה אינטגרציות מפספסות. מומש כ-best-effort, לפי הגדרת GROW עצמם |
| 16 | `data.status === 1` בלבד | ה-Light API מחזיר HTTP 200 גם בכישלון, ו-`status` חוזר פעם כמספר ופעם כמחרוזת. עכשיו הבדיקה היא על גוף התשובה, עם המרה למחרוזת |
| 17 | קריאת שדות ישירה (`data.transactionId`) | ה-callback מגיע עם מפתחות סוגריים (`data[transactionId]`) — קריאה ישירה מחזירה `null` בכל שדה, גם על תשלום אשראי אמיתי ומוצלח. נוסף `flattenBracketKeys` |
| 18 | מיפוי סטטוס: רק `'1'` | GROW מחזיר גם ערכים בעברית (`שולם`, `בוטל`, `זוכה`) ושמות שדה שונים בין ארבעה פורמטי webhook. נוסף `mapGrowStatus` + `readField` שמכיר את כל האיותים המתועדים. כל ערך לא מוכר נחשב כישלון, לעולם לא כתשלום |
| 19 | `getPaymentProcessInfo` על `api.meshulam.co.il` | הכתובת היא לפי סביבה: `sandbox.meshulam.co.il` / `secure.meshulam.co.il` |

## פערי תפעול

| # | מה היה | התיקון |
|---|---|---|
| 20 | אין הפרדת sandbox/production | `GROW_ENVIRONMENT`, וכל כתובת נגזרת ממנו. ברירת המחדל היא sandbox — בטוח כברירת מחדל |
| 21 | אין הגנה מפני מסוף בדיקה בפרודקשן | `assertLiveTerminal` + `GROW_EXPECTED_TERMINAL`. זה נכתב אחרי כשל אמיתי בפרויקט אחר על אותו שער: החשבון נשאר מכוון למסוף בדיקה אחרי עלייה לאוויר, כל חיוב דיווח הצלחה, לקוחות האמינו ששילמו, ואף שקל לא זז. שום דבר לא שגה. זה התגלה מדף חשבון בנק |
| 22 | ההזמנה לא נרשמה לפני ההפניה ל-GROW | תשלום שהצליח ב-GROW אך ה-callback שלו לא הגיע נעלם לחלוטין. עכשיו נרשם `pending_payment` לפני היציאה, וניתן להתאמה |
| 23 | ה-webhook החזיר 200 גם על שגיאה פנימית — אבל בלי לוג של הבקשה הגולמית | הבקשה נרשמת בלוג לפני כל פרשנות. במחלוקת מול לקוח על "שילמתי", זו הראיה |
| 24 | הודעות שגיאה פנימיות הוחזרו ללקוח (`error.message`) | נרשמות בלוג, לא מוחזרות |
| 25 | אין נתיב התאמה יזום | `check-grow-status` שואל את GROW ישירות ויכול לסגור הזמנה — עם אותה בדיקת סכום כמו ה-webhook |

## מה לא נבדק

הקוד לא הורץ מול GROW מהמאגר הזה — אין כאן חשבון BASE44 עם משתני סביבה. פרטי
החוט (כתובות, שמות שדות, ערכי `paymentLinkType`/`paymentTypes`) הועתקו מאינטגרציה
שכן עברה עסקאות אמיתיות. שמות השדות בישות `Order` הם הצעה: אם בפרויקט שלך הם
נקראים אחרת, יש להתאים אותם בשלוש הפונקציות (הרשימה ב-README).
